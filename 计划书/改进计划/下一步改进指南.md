# 🚀 下一步改进指南 — 听风AI（imagefree_api）

> ✅ **执行状态（2026-08-23 更新）**：v3.1.0 全部任务（S-1~S-11、S-13~S-15）已实施、测试并发布。
> GitHub Release: https://github.com/lza6/Image-to-2api/releases/tag/v3.1.0
> 关键根因修复：SQLite autocommit（WAL 读快照固定）+ lifespan DB 关闭 + loop 漂移重建——详见根目录 `workflow_status.md`。
> 以下 v3.2.0 / v4.0.0 章节仍为待实施规划。

---

## 版本路线总览

| 版本 | 状态 | 主题 |
|------|------|------|
| v3.1.0 | ✅ 已发布 | 可观测性增强 + 运维闭环 + 生产级根因链修复 |
| v3.2.0 | 待实施 | 快速胜利：CORS 安全头、契约监控、备份脚本、前端骨架屏 |
| v4.0.0 | 待实施 | 安全里程碑：管理面鉴权/限流/风控、磁盘日志 JSON 模式 |

## v3.2.0 候选任务（按影响/成本比排序）

| 编号 | 任务 | 要点 |
|------|------|------|
| V2-1 | CORS 细化 + 安全响应头 | `IF_CORS_ORIGINS` 白名单化；补 `X-Content-Type-Options: nosniff` / `X-Frame-Options: DENY` |
| V2-2 | DB 备份脚本 | `scripts/backup_db.py`：WAL checkpoint 后 copy，保留 N 份轮转；恢复流程文档 |
| V2-3 | 契约监控定时任务 | 每日跑 S-13 契约测试 → 失败触发 alerting.py 告警 |
| V2-4 | 磁盘日志 JSON 模式 | `IF_LOG_JSON=1` 结构化行输出，便于日志收集 |
| V2-5 | 覆盖率冲刺 ≥75% | main.py 校验分支 + providers 异常分支定向补测 |
| V2-6 | 前端列表页骨架屏/空态统一 | Tasks/Accounts/DLQ 复用 Feedback 组件的 Skeleton |

## v4.0.0 主题（安全里程碑，需先出威胁模型再实施）

- 管理面 API Key（默认关闭保持零鉴权哲学）
- 全局限流（令牌桶，复用 semaphore_manager 思路）
- prompt 内容过滤（黑名单关键词 → `ERROR.FILTERED`）
- 凭据热轮换脚本
- 配置指纹（diagnostics 显示 hash，变更告警）

## 实施纪律（每轮必守）

1. 先跑 `pytest tests/ -q --ignore=tests/performance` 拿绿基线再动手
2. 新配置必须同步 `.env.example` + `python scripts/sync_deploy.py sync`
3. TDD：测试先行（RED→GREEN），覆盖率目标 ≥75%
4. 不重构没坏的东西；新只读端点优先放 `api/routes/` 子包（见拆模块候选.md）
5. 收尾：版本号三处对齐（main.py / docs.html / README badge）+ workflow_status.md 更新

---

*生成：2026-08-22 · 执行更新：2026-08-23 · 由 Claude（imagefree-2ai 会话）维护*
